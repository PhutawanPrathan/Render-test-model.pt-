services:
  - type: web
    name: yolo-cloud-api
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: python cloud_server.py
    envVars:
      - key: FLASK_ENV
        value: production
    plan: free
    autoDeploy: true
